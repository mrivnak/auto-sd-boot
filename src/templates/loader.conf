{% if default %}default {{ default }}.conf{% endif %}
{% if timeout %}timeout {{ timeout }}{% endif %}
{% if editor %}editor {{ editor }}{% endif %}
{% if auto_entries %}auto_entries {{ auto_entries }}{% endif %}
{% if auto_firmware %}auto_firmware {{ auto_firmware }}{% endif %}
{% if console_mode %}console_mode {{ console_mode }}{% endif %}
