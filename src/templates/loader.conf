{% if default %}default {{ default }}{% endif %}
{% if timeout %}timeout {{ timeout }}{% endif %}
{% if editor %}editor {{ editor }}{% endif %}
{% if auto_entries %}auto-entries {{ auto_entries }}{% endif %}
{% if auto_firmware %}auto-firmware {{ auto_firmware }}{% endif %}
{% if console_mode %}console-mode {{ console_mode }}{% endif %}
